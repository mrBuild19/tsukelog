- if current_user.following?(user)
    = link_to 'フォロー外す', unfollow_path(user.id), method: :post, remote: true, class: 'btn btn-outline-primary'
- else
    = link_to 'フォローする', follow_path(user.id), method: :post, remote: true, class: 'btn btn-outline-primary'