<div class="container">
	<%= render "shared/notice", notice: notice %>
    <%= render "shared/alert", alert: alert %>

	<div class="row">
		<ul class="nav nav-pills nav-fill" role="tablist">
		  <li class="nav-item">
		    <a class="nav-link active" id="timeline-tab" data-toggle="tab" href="#timeline" role="tab" aria-controls="timeline" aria-selected="true">タイムライン</a>
		  </li>
		  <li class="nav-item">
		    <a class="nav-link" id="arrival-tab" data-toggle="tab" href="#arrival" role="tab" aria-controls="arrival" aria-selected="false">新着</a>
		  </li>
		  <li class="nav-item">
		    <a class="nav-link" id="popular-tab" data-toggle="tab" href="#popular" role="tab" aria-controls="popular" aria-selected="false">人気</a>
		  </li>
		</ul>
	</div>

	<div class="row">
		<div class="tab-content w-100">
			<!-- タイムライン -->
		  	<div class="tab-pane fade show active" id="timeline" role="tabpanel" aria-labelledby="timeline-tab">
		  		<div id="timeline-jscroll">
		  			<!-- TODO 部分テンプレート化 -->
		  			<div class="timeline-jscroll">
						<div class="row">
							<% @timeline_posts.each do |post| %>
								<%= render "post_card", post: post %>
							<% end %>
						</div>
					</div>
				</div>
				<!-- TODO 部分テンプレート化 -->
				<%= link_to_next_page @timeline_posts, '次のページ', class: "timeline-next" %>
				<%= render "shared/scroll_setting" %>
			</div>

			<!-- 新着 -->
			<div class="tab-pane fade show" id="arrival" role="tabpanel" aria-labelledby="arrival-tab">
	  			<div id="arrival-jscroll">
	  				<!-- TODO 部分テンプレート化 -->
	  				<div class="arrival-jscroll">
						<div class="row">
							<% @arrival_posts.each do |post| %>
								<%= render "post_card", post: post %>
							<% end %>
						</div>
					</div>
					<%= link_to_next_page @arrival_posts, '次のページ', class: "arrival-next" %>
	  			</div>
	  			<!-- TODO 部分テンプレート化 -->
	  			<button class="arrival-button" type="button">もっと見る</button>
				<%= render "shared/scroll_setting" %>
		  	</div>

		  	<!-- 人気 -->
			<div class="tab-pane fade show" id="popular" role="tabpanel" aria-labelledby="popular-tab">
				<div id="popular-jscroll">
					<!-- TODO 部分テンプレート化 -->
	  				<div class="popular-jscroll">
						<div class="row">
							<% @popular_posts.each do |post| %>
								<%= render "post_card", post: post %>
							<% end %>
						</div>
					</div>
	  				<%= link_to_next_page @popular_posts, '次のページ', class: "popular-next" %>
	  			</div>
	  			<!-- TODO 部分テンプレート化 -->
	  			<button class="popular-button" type="button">もっと見る</button>
	  			<%= render "shared/scroll_setting" %>
			</div>
		</div>
	</div>
</div>